package com.com.wxscistor;

import com.dl.DiyGraph;
import com.dl.QuerTest;
import org.vertexium.Graph;

public class Child extends AbsClassA{
    public Child(Graph graph, DiyGraph diyGraph, QuerTest querTest) {
        super(graph, diyGraph, querTest);
    }
}
